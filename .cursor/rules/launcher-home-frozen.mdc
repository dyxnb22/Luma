---
description: Frozen launcher home — Open Apps left column; guide/detail right; no suggestion rows
globs: Sources/LumaApp/Launcher/**,Sources/LumaCore/Home/**,docs/ENGINEERING.md,docs/MODULES.md,docs/DECISIONS.md,docs/QA.md
alwaysApply: true
---

# Launcher home (frozen)

Read **`docs/ENGINEERING.md`**, **`docs/MODULES.md`**, and **`docs/DECISIONS.md`** before changing home UI, panel size, or list chrome.  
Manual verification lives in **`docs/QA.md`**.

## Empty-query layout (visible query empty)

- **Left column (280 pt):** Open Apps — all running apps, no `+N more` row.
- **Right column:** command guide (home) **or** module detail — not a second navigable list.
- Module detail must **not** hide or cover the left Open Apps column.
- Typing any query → single-column search results (split off).

## Do NOT restore without a new ADR

- Setup / getting-started rows or `SetupHomeProvider` on home
- Recent / 最近, Continue / 继续, Create / 创建 sections
- Auto-present onboarding wizard
- Gray background on every idle list row
- Full-panel detail overlay when visible query is empty

## Panel

- Default **940 × 760 pt**, left column **280 pt**, upper-third (`panelVerticalBias` 0.68).
- Tokens: `LauncherChromeTokens.swift`.
- Positioning + in-panel layout: **`docs/ENGINEERING.md`**.

## Where features go instead

- Search, module prefixes (`clip`, `t`, `word`, …), bare commands, module detail (right column), `proj` workbench commands.

## PR requirements

If adding home sections or changing split layout: update `docs/ENGINEERING.md`, `docs/MODULES.md`, `docs/DECISIONS.md`, and `docs/QA.md` if behavior changes.
